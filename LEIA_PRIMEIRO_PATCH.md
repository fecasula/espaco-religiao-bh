# Patch v0.1.12

Extraia o conteúdo deste diretório na raiz do projeto, preservando as pastas e substituindo os arquivos existentes.

Em seguida, reinicie o R e execute:

```r
source("scripts/08_prepare_shiny_lockfile.R")
source("scripts/07_run_tests.R")
source("scripts/05_deploy_shiny.R")
```
