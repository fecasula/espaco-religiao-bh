# Projeto Religião e Mobilidade — versão 0.1.12

Esta versão corrige a publicação no shinyapps.io gerando um `manifest.json` com `metadata$appmode = "shiny"` e preenchendo explicitamente os campos `Github*` de todos os pacotes provenientes do GitHub.

Consulte `INSTRUCOES_DE_ATUALIZACAO_v0.1.12.md` antes do novo deploy.
